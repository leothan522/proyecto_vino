<p class="d-flex">
    <span>Subtotal</span>
    <span class="text-right">${{ formatoMillares($subtotal) }}</span>
</p>
<p class="d-flex">
    <span>Entrega</span>
    <span class="text-right">${{ formatoMillares($entrega) }}</span>
</p>
<hr>
<p class="d-flex total-price">
    <span>Total</span>
    <span class="text-right">BCV ${{ formatoMillares($total) }}</span>
</p>
